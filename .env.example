# Copy to .env and fill in values. Never commit .env.
#
# Development (EDU) DB
DB_HOST=
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=
#
# Production DB (read-only usage in code paths that reference *_PROD)
DB_HOST_PROD=
DB_PORT_PROD=3306
DB_USER_PROD=
DB_PASSWORD_PROD=
DB_NAME_PROD=
